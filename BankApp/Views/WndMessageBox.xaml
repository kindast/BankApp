<Window x:Class="BankApp.Views.WndMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BankApp.Views"
        mc:Ignorable="d"
        Title="" Height="130" Width="250" ResizeMode="NoResize" 
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="tbMessage" Style="{StaticResource BaseTextBlock}" FontSize="14" Margin="10,5,10,5"
                   TextWrapping="Wrap"/>
        <Button Content="OK" Style="{StaticResource BaseButton}" 
                Grid.Row="1" FontSize="14" Height="30" Width="50"
                HorizontalAlignment="Right" Margin="0,5,10,5"
                Click="Button_Click" Cursor="Hand" FontWeight="Light"/>
    </Grid>
</Window>
